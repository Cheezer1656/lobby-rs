# Lobby-rs
Lobby-rs is a fast, lightweight, and customizable Minecraft lobby server written in Rust. It is built on top of [Valence](https://github.com/valence-rs).

## Getting started
1. Clone the repo
2. Go into the `example_config` directory
3. Run `cargo run`
4. Join `localhost:25565`
